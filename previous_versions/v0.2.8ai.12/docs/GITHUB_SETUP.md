# GitHub / release notes

- Release tag: `v0.2.8ai`
- Release title: `v0.2.8ai — 11 Controls + AI-DDSP`
- Do not include generated `config.json`, `.venv`, engine locks/PID files, logs, or global registry data in a public package.
- Do include `requirements.lock.txt`; it defines the reproducible RC3.3 runtime environment.
- Voicebank `.yuaz*` state is user data and must never be bundled into the program release.
